'use strict';

var pstore = 'protodb-people';
var gstore = 'protodb-groups';
var sstore = 'protodb-sessions';
var cstore = 'protodb-config';

var dbshowcols = ['Staff Name', 'Student Name', 'Student ID', 'Grade',
                    'Age', 'sesname']
